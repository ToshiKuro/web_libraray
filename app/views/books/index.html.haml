%h1 書籍一覧
%table{border: 1}
  %tr
    %th タイトル
    %th 著者名
    %th 発刊日
    %th 内容
  - @books.each do |book|
    %tr
      %td= link_to book.title, book_show_path(book)
      / %td= link_to book.name, "books/#{book.id}"
      %td= book.author
      %td= book.date_of_issue
      %td= book.summary
