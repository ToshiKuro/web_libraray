%h2 書籍一覧
%table{border: 1}
  %tr
    %th タイトル
    %th 著者名
    %th 分類
    %th 在架数
    %th user id
    %th 
  - @books.each do |book|
    %tr
      %td= link_to book.title, book_show_path(book)
      / %td= link_to book.name, "books/#{book.id}"
      %td= book.author
      %td= book.genre
      %td= book.number
      %td= book.user_id
      -if book.number > 0
        %td= link_to book.rend, book_rend_path(book)
      -else
        %td= ''

